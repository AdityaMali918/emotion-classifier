 1. dvc add stage
 -n name
 -d dependency
 -p parmeter
 -o data/raw


 YAML - YAML aint a markup language
 1. More readable
 2. more data types
 3. comments


COMMAND : dvc stage add -n data_ingestion -d src/data_ingestion.py -o data/raw python src/data_ingestion.py

 eg:
 stages:
    data_ingestion
        cmd: python src/data.py
        deps:
        - src/data.py
        outs:
        - data/raw

2. Run this dvc.yaml file  -> dvc repro  (reproduce)

3. dvc dag -> Is used to visualize the steps (directed acyclic graph)

4. dvc stage add -n data_preprocessing -d data/raw -d src/data_preprocessing.py -o data/processed --force python src/data_preprocessing.py

5. --metrics is used show the metrics of the file
   dvc metrics show
   dvc stage add -n model_evaluation -d src/model_evaluation.py -d model.pkl --metrics metrics.json --force python src/model_evaluation.p

6. pip install cookiecutter
cookiecutter https://github.com/drivendataorg/cookiecutter-data-science   

7. git tag
git tag -a v1.0 -m "Release v1 with BOW"
git push origin main
git push origin v1.0
git tag
git show v1.0

8. git checkout tags/v1.0

9.
 1. git init 
 2. dvc init
      .dvc 
         temp
         config
         .gitignore
      .dvcignore   

 3. dvc remote add -d name storagepath eg(D://data)        

10 dvc add data/customer.csv (multiple thing are happening here)   

11. dvc push

steps : 
   1. new data added
   2. dvc add data/customer.csv
   3. git add . 
   4. git commit -u "Added the data"
   5. dvc push
   6. git push -u origin main 

12 changing code and data
   First do 1. git checkout hashkey
   Second do 2. dvc checkout (Be carefull you have to do this as the data will remain previous commit)   

13 if somebody else start working on our repository then

1. git clone
2. dvc fetch 
3. dvc checkout

If somebodywant to switch between version
1. git checkout task
2. dvc fetch
3. dvc checkout 

dvc pull = dvc checkout + dvc pull